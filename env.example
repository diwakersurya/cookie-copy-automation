# Chrome DevTools Protocol Configuration
CDP_URL=http://localhost:9222

# Navigation Configuration
START_URL=https://admin.example.com/login
EXPECT_URL_INCLUDES=/dashboard

# Cookie Configuration
COOKIE_NAME=sosense

# Tab Selection (optional)
PICK_TAB_URL_INCLUDES=
PICK_TAB_TITLE_INCLUDES=

# Interaction Configuration
AGENCY=multientity
PROJECT=

# Custom Selectors
CUSTOM_SELECT_OPEN_SELECTOR=
CUSTOM_SELECT_OPTION_SELECTOR=
LOGIN_UL_SELECTOR=
LOGIN_ANCHOR_SELECTOR=#side-menu > li.active > ul > li:nth-child(3) > a
SUBMIT_BUTTON_SELECTOR=button[type="submit"], button[data-testid="continue"], [data-action="login"]

# Timeouts (in milliseconds)
NAV_TIMEOUT=120000
IDLE_TIMEOUT=15000

# Logging Configuration
VERBOSE=false
QUIET=false

# Clipboard Configuration
COPY_TO_CLIPBOARD=true
